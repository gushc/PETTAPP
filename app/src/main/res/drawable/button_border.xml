<?xml version="1.0" encoding="utf-8"?>



<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Efecto ripple al tocar -->

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /><!-- Color de fondo marrón -->
            <corners android:radius="20dp" /> <!-- Bordes redondeados -->
            <stroke
                android:background="@color/grisoscuro"
                android:width="3dp"
                android:color="@color/colorbase" />  <!-- Color del borde marrón -->
        </shape>
    </item>
</ripple>
